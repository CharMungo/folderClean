A simple shell script that automatically searches through a directory and deletes all files that (0-9) before the file extension.

## Options

folderClean (Directory) (filetype to search for) (delete files without an original? y/n)
